---
- hosts: "{{ host_server }}"
  gather_facts: yes
  vars_files:
    - AnsibaleTest/vars/global.yaml 
  roles:
    - AnsibaleTest/roles/restore_db/tasks/main.yml
   # - { role: restore_db,
   #    tags: [ restore_db ] } 
